To train the model, type 'python GAN.py' into a command line.

This script was verified to work using Python 3.12.4 and PyTorch v2.3.1+cu121.

To set up your environment, create a virtual environment and install the packages from requirements.txt using 'pip install -r requirements.txt'. 